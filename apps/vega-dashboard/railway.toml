[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/vega-dashboard/Dockerfile"

[deploy]
healthcheckPath = "/admin"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
